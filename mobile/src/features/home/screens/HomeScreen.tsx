import React from 'react';
import {ScreenScaffold} from '@/common/ui/ScreenScaffold';

export function HomeScreen(): React.JSX.Element {
  return (
    <ScreenScaffold
      title="Khasahi AI"
      description="The home feature will prioritize scan entry points, recent history, and profile-aware guidance."
    />
  );
}

