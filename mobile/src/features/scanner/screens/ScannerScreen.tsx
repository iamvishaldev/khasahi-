import React from 'react';
import {ScreenScaffold} from '@/common/ui/ScreenScaffold';

export function ScannerScreen(): React.JSX.Element {
  return (
    <ScreenScaffold
      title="Scanner"
      description="Vision Camera, live barcode scanning, OCR capture, and graceful recovery states will be implemented here."
    />
  );
}

